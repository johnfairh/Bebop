// swift-tools-version:5.1

import PackageDescription

let package = Package(
    name: "ByPath",
    products: [
        .library( name: "ByPath", targets: ["ByPath"])
    ],
    targets: [
        .target( name: "ByPath", dependencies: [])
    ]
)
