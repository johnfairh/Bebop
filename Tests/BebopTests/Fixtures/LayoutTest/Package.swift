// swift-tools-version:5.1

import PackageDescription

let package = Package(
    name: "Layout",
    products: [
        .library(
            name: "Layout",
            targets: ["Layout"]
        )
    ],
    targets: [
        .target(
            name: "Layout",
            dependencies: [])
    ]
)
