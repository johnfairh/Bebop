# Jazzy Incompatibilities

### Deleted Options

* `--swift-version`.  Set `DEVELOPER_DIR` manually.
* `--use-safe-filenames`.  Delete: filenames are no longer dangerous.
* `--template-directory`.  Use `--theme`.
* `--assets-directory`.  Use `--theme`.
